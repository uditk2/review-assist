{
  "name": "review-assist",
  "version": "0.1.0",
  "private": true,
  "description": "Intent Document \u2014 an open format for AI change-intent, with a validator, MCP server, and guided-review viewer.",
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*",
    "apps/github-app/worker",
    "apps/github-app/viewer"
  ],
  "scripts": {
    "build": "npm run build -w @review-assist/schema && npm run build -w @review-assist/validator && npm run build -w review-assist-mcp && npm run build -w @review-assist/viewer",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "@fontsource/ibm-plex-mono": "^5.3.0",
    "@fontsource/ibm-plex-sans": "^5.3.0",
    "@types/node": "^22.20.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
